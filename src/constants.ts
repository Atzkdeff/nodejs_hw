/**
 * Array with possible group permissions
 */
export const groupPermissions: string[] = ['READ', 'WRITE', 'DELETE', 'SHARE', 'UPLOAD_FILES'];

/**
 * JWT secret
 */
export const SECRET: string = 'TEMPORARY_SECRET';
